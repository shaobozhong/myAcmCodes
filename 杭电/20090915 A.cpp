#include<iostream>
using namespace std;

int main()
{
	
	int a[9],sum;
	int wb,wr,wg,wy,w,test=0;
	while(scanf("%d",&wb)>0&&wb!=0)
	{
		++test;
		sum;
		scanf("%d%d%d%d",&wr,&wg,&wy,&w);
		if (w>3*wb+3*wr+2*g+wy) {printf("Impossible\n");continue;}
		a[0]=wb;
		a[1]=wr;
		a[2]=wb;
		a[3]=wr;
		a[4]=wg;
		a[5]=wy;
		a[6]=wb;
		a[7]=wg;
		a[8]=wr;
		for(i=0;i<1;i++)
		{
			sum+=a[i];
			if (sum>=w) break;
		}
		if sum
	}
}